<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-lg">
        <div class="card-body">
          <h2 class="text-center mb-4">My Tasks</h2>

          <ul class="list-group">
            <% @tasks.each do |task| %>
              <li class="list-group-item d-flex justify-content-between align-items-center">
                <div>
                  <%= check_box_tag "task_#{task.id}", task.id, task.completed, class: "form-check-input me-2" %>
                  <%= link_to task.title, task_path(task), class: "text-decoration-none fw-bold" %>
                </div>
                <%= link_to "✖", task_path(task), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-sm btn-outline-danger" %>
              </li>
            <% end %>
          </ul>

          <div class="mt-4 text-center">
            <%= link_to "+ Add new task", task_new_path, class: "btn btn-primary btn-lg" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
